// Application constants

// Auto-scroll settings
export const AUTO_SCROLL_BASE_INTERVAL = 50; // Base interval for normal scroll (ms)
export const AUTO_SCROLL_SPEED_STEPS = [1, 1.25, 1.5, 2]; // Speed multipliers

// Socket connection settings
export const SOCKET_JOIN_DELAY = 200; // Delay before emitting join event (ms)

// UI Settings
export const DEFAULT_TIMEOUT = 120000; // Default timeout for operations (ms)